from django.conf.urls import patterns, include, url
from attendence import views
from django.contrib.auth.forms import AuthenticationForm

urlpatterns = patterns('',
    url(r'^(?P<docid>\d+)?$',views.home,name='attend_home'),

)
